#include "config.h"


namespace Config { 
    const float GRAVITY = 50.0f;
    const float GRAVITATIONAL_CONST = 50.0f;
    const float TIME_STEP  = 1.0f/60.0f //60 FPS
    const int WINDOW_WIDTH = 1200;
    const int WINDOW_HEIGHT = 900;
    const float G_SOFTENING = 0.2f; // Prevents instability at small distances
}

